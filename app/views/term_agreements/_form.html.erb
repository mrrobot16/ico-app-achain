  <%= form_with(model: term_agreement, local: true) do |form| %>
    <% if term_agreement.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(term_agreement.errors.count, "error") %> prohibited this term_agreement from being saved:</h2>

        <ul>
        <% term_agreement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <h1>Term and Conditions</h1>
    <div class='terms'>
      <div class="field">
        <p>Text fill with terms number 1</p>
        <%= form.label :check_one %>
        <%= form.check_box :check_one, id: :check_one %>
      </div>
      <div class="field">
        <p>Text fill with terms number 2</p>
        <%= form.label :check_two %>
        <%= form.check_box :check_two, id: :check_two %>
      </div>
      <div class="field">
        <p>Text fill with terms number 3</p>
        <%= form.label :check_three %>
        <%= form.check_box :check_three, id: :check_three %>
      </div>
    <div>
    <div class="field">
      <%= form.label :shareholder_address %>
      <%= form.text_field :shareholder_address, id: :term_agreement_shareholder_address %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
